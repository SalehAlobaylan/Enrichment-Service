fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0
structlog>=24.0.0

# ── PyTorch CPU-only ─────────────────────────────────────────
# The default torch wheel pulls ~3 GB of nvidia-* CUDA libraries
# (cublas, cudnn, nccl, cufft, cusolver, cusparse, triton, etc.) that
# we never use on Cranl's CPU instances and that blow past the build
# disk quota. Pin the CPU wheel explicitly via PyTorch's CPU index.
# sentence-transformers + FlagEmbedding both pull torch as a transitive
# dep — putting torch ABOVE them ensures the CPU pin wins.
# See https://pytorch.org/get-started/locally/ for index conventions.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0,<3.0

# transformers MUST stay on 4.x. FlagEmbedding's reranker (FlagReranker.
# compute_score) calls the tokenizer's `prepare_for_model`, which transformers
# v5 removed → "XLMRobertaTokenizer has no attribute prepare_for_model" and
# every rerank/news-slide call 500s. FlagEmbedding's own pin (<6) is too loose.
transformers>=4.44.2,<5.0
sentence-transformers>=3.0.0
FlagEmbedding>=1.2.0

# Single-URL extraction (/v1/extract): Scrapling stealth fetch (curl_cffi
# browser TLS-impersonation gets past Cloudflare/Akamai bot walls — no
# Playwright/Chromium needed). curl_cffi + browserforge are Scrapling's
# fetcher-engine deps that its 0.4.x wheel FAILS to declare; their absence is
# exactly what broke /v1/extract in prod, so we pin them explicitly. lxml
# (also Scrapling's own dep) parses RSS/Atom feed XML CDATA-safely.
scrapling>=0.4.0,<0.5
curl_cffi>=0.7.0
browserforge>=1.2.0
lxml>=5.0.0

openai>=1.50.0
anthropic>=0.37.0
google-genai>=1.0.0
prometheus-fastapi-instrumentator>=7.0.0
python-multipart>=0.0.12
redis>=5.0
