# Discovery adapter fixtures

These fixtures are deliberately synthetic, minimal parser shapes. They contain
no credentials, session data, tracking parameters, or captured public content.
When an upstream changes, first add the smallest failing shape here (or inline
when its shape is clearer), then adapt the parser without replacing prior cases.

Live adapter smoke checks are intentionally outside required CI.
