-r requirements.txt

# Test + lint toolchain
pytest>=8.0
pytest-asyncio>=0.24.0
respx>=0.21.0
coverage>=7.0
ruff>=0.7.0

# Playwright lives here, not in production requirements.txt — the prod
# Docker image runs Scrapling in plain-HTTP mode to save ~500 MB of
# Chromium + system deps. Local dev keeps Playwright so /v1/extract can
# render JS-heavy pages while iterating.
playwright>=1.48.0
